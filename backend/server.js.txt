const express = require('express');
const app = express();
const port = 3000;

app.use(express.static('../'));

app.get('/api/shops', (req,res)=>{
    res.json({
        message:"API for shops will be implemented here"
    });
});

app.listen(port, ()=>{
    console.log(`Server running at http://localhost:${port}`);
});
